This project uses a root-level Gradle wrapper that delegates to the Android module's wrapper JAR.
The root gradlew script will locate and use the module's gradle/wrapper/gradle-wrapper.jar dynamically.
No binary files are duplicated here.
