# CI Init

If the CI runner cannot execute `./gradlew` from the repository root, use:

- bash .init/build_with_module_wrapper.sh

This script changes to `music_dj_creator_frontend/` and runs the module's Gradle wrapper.
